<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- ValidationRuleConfiguration Table -->
    <!-- Stores configurable validation rules for custom field types (FR-018) -->
    <changeSet id="analyzer-013-create-validation-rule-configuration-table" author="astm-analyzer-mapping-feature">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="validation_rule_configuration"/>
            </not>
        </preConditions>
        <comment>Creating validation_rule_configuration table for custom field type validation rules</comment>
        <createTable tableName="validation_rule_configuration">
            <column name="id" type="VARCHAR(36)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="custom_field_type_id" type="VARCHAR(36)">
                <constraints nullable="false"
                    foreignKeyName="fk_validation_rule_custom_field_type"
                    references="custom_field_type(id)"
                    deleteCascade="true"/>
            </column>
            <column name="rule_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="rule_type" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="rule_expression" type="TEXT"/>
            <column name="error_message" type="VARCHAR(500)"/>
            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="sys_user_id" type="VARCHAR(36)"/>
            <column name="last_updated" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <sql>
            ALTER TABLE validation_rule_configuration
            ADD CONSTRAINT chk_rule_type
            CHECK (rule_type IN ('REGEX', 'RANGE', 'ENUM', 'LENGTH'));
        </sql>
        <createIndex indexName="idx_validation_rule_custom_field_type_id"
                     tableName="validation_rule_configuration">
            <column name="custom_field_type_id"/>
        </createIndex>
        <rollback>
            <dropTable tableName="validation_rule_configuration"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
